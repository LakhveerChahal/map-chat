import { ItBtnDirective } from './it-btn.directive';

describe('ItBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ItBtnDirective();
    expect(directive).toBeTruthy();
  });
});
